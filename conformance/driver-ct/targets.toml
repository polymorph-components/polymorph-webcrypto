version = "0.1"

[features.sha1-checked]
kind = "gated"
[features.ecdsa-sign]
kind = "structural"

[features.rsa-verify-8192]
# RSASSA-PKCS1-v1_5 verification with an IMPORTED 8192-bit public key —
# the whole `rsassa-pkcs1-v15-sha256-8192/wycheproof` row, both import
# paths. A platform capability, not a policy: a host cannot work around
# a key its platform refuses to import.
#
# The capability probe — verify the platform serves an imported 8192-bit
# public key. Vectors: `rsa_signature_8192_sha256_test.json`, its single
# group (`g`); `t` is any of that group's `valid` tests:
#
#   const alg = { name: "RSASSA-PKCS1-v1_5", hash: g.sha };
#   const k = await crypto.subtle.importKey(
#     "spki", hex(g.publicKeyDer), alg, false, ["verify"]);
#   await crypto.subtle.verify(alg.name, k, hex(t.sig), hex(t.msg));
#
# Observed split: Deno 2.9.5 throws at importKey — "public key error:
# SPKI cryptographic key data malformed" — and identically for
# importKey("jwk", g.keyJwk), while generateKey({modulusLength: 8192})
# succeeds, so it is the import of oversized moduli that is missing, not
# the size. Node 24 and Chromium (the jco legs) import and verify.
kind = "gated"

[targets.wasmtime-rustcrypto]
missing-features = []

[targets.composed]
# The shared suite plugged with the in-guest RustCrypto provider
# (rust/guest-provider), run under the generic component-test host
# runner. The provider serves every gated feature the shared suite
# exercises (sha1-checked); its one deliberate gap is `ecdsa-sign`
# (class D — see rust/guest-provider/README.md), which nothing in this
# suite is tagged with (the exclusion is structural: the signing suite's
# world imports the interface, so it cannot even link composed —
# `just class-d` asserts the absent export, so this line cannot outlive
# the fact it records).
missing-features = ["ecdsa-sign"]

[targets.jco-node]
missing-features = ["sha1-checked"]

[targets.jco-browser]
# The same host module as jco-node, in headless Chromium. Like jco-node it
# cannot serve `sha1-checked`. The browser also fails the gated RSA
# private-key mints closed (see targets-signing.toml; nothing in this
# suite is tagged with them).
missing-features = ["sha1-checked"]
# Gates in CI (the Actions runner ships Chrome); locally it needs a
# Chrome/Chromium install and runs only when opted in with
# CONFORMANCE_BROWSER=1, so missing results files are warnings, not errors.
optional = true

[targets.deltic-deno]
# The same reference host as jco-node/jco-browser in behavior, rewritten
# over deltic's embedder API (js/deltic/src/mod.ts), run RUNTIME-LINKED
# under stock Deno — no transpile step, no generated tree. Like every
# platform-WebCrypto-backed target it cannot serve `sha1-checked` (no
# platform carries sha1dc), and the suite's `!sha1-checked` decline case
# verifies the refusal.
missing-features = ["sha1-checked", "rsa-verify-8192"]
# `rsa-verify-8192` replaced this target's 299 per-case expected-fail
# entries for the 8192-bit RSASSA row (polymorph-webcrypto#351): the gap
# is the whole row, so it is named as a capability the target lacks and
# proved by the `!rsa-verify-8192` decline case, rather than ledgered
# case by case. The census fixture records the row's tag; see the
# amendment note in conformance/guest-ct/src/census-fixture.lock.
#
# What remains below is the debt that is genuinely partial-row: Deno's
# WebCrypto is narrower than Node's and the browsers' in three
# parameter windows the WIT admits (AES-GCM tag/IV lengths, AES-CTR
# counter width) plus one OAEP label encoding; those cases are declared
# below as expected failures — tracked debt, not silent tolerance, and
# the aggregate fails on any UNdeclared failure or any declaration that
# has gone stale.
[[targets.deltic-deno.expected-fail]]
case = "probe/gcm-full-parameters"
reason = "Deno WebCrypto: AES-GCM decrypt refuses tags shorter than 128 bits"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "probe/gcm-nonce-window"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits (WIT window 12-128 bytes)"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "probe/ctr-known-answers"
reason = "Deno WebCrypto: AES-CTR counter width limited to 32/64/128 bits (WIT window 1-128)"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc259/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 120"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc259/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 120"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc259/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 120"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc260/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 160"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc260/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 160"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc260/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 160"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc263/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 120"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc263/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 120"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc263/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 120"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc264/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 160"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc264/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 160"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc264/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 160"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc265/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 256"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc265/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 256"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc265/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 256"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc266/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 512"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc266/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 512"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc266/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 512"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc267/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 1024"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc267/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 1024"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc267/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 1024"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc273/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 256"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc273/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 256"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc273/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 256"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc274/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 512"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc274/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 512"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc274/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 512"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc275/whole"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 1024"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc275/bytes"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 1024"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"

[[targets.deltic-deno.expected-fail]]
case = "aes-gcm/wycheproof/tc275/straddle"
reason = "Deno WebCrypto: AES-GCM IV window is 96/128 bits; this case uses 1024"
tracking = "https://github.com/polymorph-components/polymorph-webcrypto/issues/351"