{
  "name": "@polymorph/webcrypto-polyengine-inject-probe",
  "private": true,
  "type": "module",
  "description": "Playwright driver for the injection probe: the polyengine host module's webcryptoHost().inject API against a real Chromium's Web Crypto.",
  "dependencies": {
    "playwright-core": "1.62.1"
  }
}
