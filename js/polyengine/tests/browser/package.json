{
  "name": "@polymorph/webcrypto-polyengine-browser-probe",
  "private": true,
  "type": "module",
  "description": "Playwright driver for the keystore browser probe: js/polyengine's polymorph:webcrypto-keystore host module against a real Chromium's IndexedDB + Web Crypto, across a real page reload.",
  "dependencies": {
    "playwright-core": "1.62.1"
  }
}
